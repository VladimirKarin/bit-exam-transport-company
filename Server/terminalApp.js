const { createContainer } = require('./models/containers');
const { createUser } = require('./models/users');

// createUser('admin', '202cb962ac59075b964b07152d234b70', 'Administrator');
createContainer('small');
